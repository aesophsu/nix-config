{
  myvars,
  lib,
}:
let
  # =====================================================================================
  # Test scope
  # =====================================================================================

  username = myvars.username;

  hosts = [
    "fern"
    "frieren"
    "stella"
  ];

in
# =====================================================================================
# Expected home directory layout per host
# =====================================================================================

lib.genAttrs hosts (_: "/Users/${username}")
